import React from 'react'
import { StyleSheet, View, Text } from 'react-native'

const Login = props => {
    return (
        <View style={styles.page}>
            <Text>Login Form will be here soon...</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    page: {
        alignItems: "center",
        justifyContent: "center",
        height: "100%"
    }
});

export default Login